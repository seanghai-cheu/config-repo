server:
  port: 8082

eureka:
  client:
    serviceUrl:
      defaultZone: http://admin:admin@localhost:8761/eureka/
  instance:
    instanceId: camel-service:${vcap.application.instance_id:${spring.application.instance_id:${random.value}}}