server:
  port: 8081

eureka:
  client:
    serviceUrl:
      defaultZone: http://admin:admin@localhost:8761/eureka/
  instance:
    instanceId: hello-service:${vcap.application.instance_id:${spring.application.instance_id:${random.value}}}